# 2026-05-07 Memory

## Course home workflow correction

- User corrected the role boundary: current Codex is the workflow/DAG layer and must not push workflow gaps back to another role for discussion.
- Verified current repository state: `.agent/templates/course-app/src/views/CourseHome.vue` exists, `CourseContent/Module_01` through `Module_04` exist, and `MVPMCP` assembles templates into `CourseApp`.
- Older execution-layer claims that templates or Module_02/03/04 were missing are stale for the current repo state.
- Added `.agent/design/course-home-progress-contract.json` to define learner progress state, localStorage key, state dependency semantics, and module metadata policy.
- Added `.agent/tasks/course-home-module-progress-maker-brief.md` so the execution layer has a file-based instruction source for regenerating and verifying the homepage.
- Extended `scripts/verify_course.py` and `.agent/templates/scripts/verify_course.py` to require the progress contract and maker brief.
- Product artifacts were not hand-edited.

# 2026-05-05 工作日志

1. **检查 WorkBuddy 作业与遗留问题**
   - 目标：检查 WorkBuddy 做了什么、遇到哪些问题，以及当前是否仍有违规或验证阻塞。
   - 发现：WorkBuddy 主要处理了 visualSpecs/animation performance 接入、MVP node drift 修复、storyboard/design/mvp MCP 逻辑修复，并生成了当天 handoff；同时曾写入 `.workbuddy/memory/2026-05-05.md` 触发平台违规报告。
   - 修复：删除空 `.workbuddy/` 目录；重新生成 Module_01 的 public audio/subtitle 产物；创建当天 `.agent/memory/2026-05-05.md`。
   - 验证：待执行 `python .agent\platform_violation_guard.py --basedir .`、`python scripts\verify_course.py`、`npm --prefix CourseApp run build`。
   - 风险：WorkBuddy 的 handoff 文档仍存在 mojibake显示痕迹，需以后优先保持 UTF-8 可读交接。

2. **修复 WorkBuddy flow-engine 回归**
   - 目标：回应"修复！"，修掉 WorkBuddy 遗留的 DesignMCP/v0MCP 执行阻塞，让 flow engine 重新跑通。
   - 修改：修复 `.agent/mcp_servers/design_mcp.py` 中缺失的 `design_slides` 初始化、`_infer_typography()`、`_infer_color_scheme()`、错误的 diagram keyword 判断、被坏注释吞掉的 `palette` 赋值，以及多处导致 SyntaxError 的 mojibake 字符串；修复 `.agent/mcp_servers/v0_mcp.py` 的坏 f-string 和乱码字符串，新增 `V0_API_TIMEOUT_SECONDS` 与 `local-v0-fallback`。
   - 验证：`V0_API_TIMEOUT_SECONDS=5 python -u .agent\flow_engine.py --mode test --stage mvp --scope module --module Module_01 --basedir .` 跑到 `DEPLOY_READY`；`python .agent\platform_violation_guard.py --basedir .`、`python scripts\verify_course.py`、`npm --prefix CourseApp run build` 全部通过。
   - 风险：本次 v0 远程 chat 创建超时，因此 v0 产物为本地 fallback handoff；如后续强依赖真实 v0 视觉稿，需要单独重试远程 v0。
   - 下一步：刷新本地页面，继续检查 p01、p01/explore 和 quiz 的视觉与交互。

3. **修复 animation.parameters.scale 缺失 和 验证 colors 数据**
   - 目标：修复 P1 动画增强不生效问题（scale 动画 + 动态阴影），并验证 `performanceSpecs[].payload.colors` 是否正确生成。
   - 根因：`storyboard_mcp.py` 的 `_motion_cues()` 中 `animation["parameters"]` 缺少 `scale` 字段，导致 `SlideCanvas.vue` 的 P1 动画增强（scale + 动态阴影）不生效。
   - 修复：在 `animation["parameters"]` 中添加 `"scale": [0.96, 1.0]`（第 642 行）。
   - 验证：重新生成 `storyboard-contract.json`，确认 `visualSpecs[].animation.parameters.scale = [0.96, 1.0]`。
   - 验证：确认 `performanceSpecs[].payload.colors` 正确生成（非空数组），例如 p00 的 `colors=['#67e8f9', '#818cf8', '#c084fc']`。
   - 自检：`verify_course.py` ✅ 通过，`npm run build` ✅ 通过。
   - Git 提交：`8efc269`。
   - 状态：P1 动画增强现在应该生效（scale 动画 + 动态阴影），ParticleDecoration.vue 能读到 `payload.colors`。

## 2026-05-05 Session 3 — 全模块生产

### 用户指令
"读rule 生产所有模块，完整开发课程。执行！"

### 执行记录
1. 读 `.agent/rules.md` 对齐约束
2. Git 快照：`b7151e0` Snapshot: Pre-All-Modules
3. 创建 Module_02/03/04 源文件（CourseContent/）：
   - Module_02: MaterialPropertyDrawer 基础（2 slides）
   - Module_03: 自定义 ShaderGUI 开发（2 slides）
   - Module_04: 高级布局与实战技巧（2 slides）
4. 更新 `.agent/mvp-scope.json` 包含所有 4 个模块
5. 平台违规修复：`.workbuddy/memory/` 迁移到 `.agent/memory/`，删除违规目录
6. 停止 dev server → 逐模块运行 MVP（01/02/03/04）
   - 全部返回 DEPLOY_READY ✓
7. Git 提交推送：`aa13bc8` feat: 全模块生产完成 Module_01-04 MVP 通过
8. Dev server 重启：http://localhost:5173/ → 200 ✓
9. `verify_course.py` 通过 ✓

### 全课程结构
- Module_01: ShaderGUI：不只是换一层皮（已有，重新生成）
- Module_02: MaterialPropertyDrawer 基础
- Module_03: 自定义 ShaderGUI 开发
- Module_04: 高级布局与实战技巧

### 待完善
- Module_02/03/04 的逐字稿为 MVP 自动生成，内容可能较简，需后续人工润色
- explorations 页面仅 Module_01 有定义，02/03/04 为空
- 表演动画（FlowPathDemo）仅在 Module_01 p01 有配置，其他模块未配置

---

## 4. 创建 ADPMCP 平行 DAG 节点

### 用户指令
"建一个 adp_mcp 跟 mvp_mcp 平级的 DAG 节点"

### 执行记录
1. 读 `.agent/rules.md` 对齐约束（之前忽略了，被用户批评）
2. 恢复 `mvp_mcp.py` 到原始状态（之前错误修改了它）
3. 创建 Git 快照：`bb36efa` Snapshot: Pre-ADP
4. 创建新文件 `.agent/mcp_servers/adp_mcp.py`（平行节点，不修改 `mvp_mcp.py`）
   - 类名为 `ADPMCP`
   - 读取 `adp-scope.json`（而非 `mvp-scope.json`）
   - 生成所有 slides（完整生产，非 MVP 裁剪）
5. 更新 `flow_engine.py`：
   - 导入 `ADPMCP`
   - 添加 `mode` 参数到 `CorePipeline.run()`
   - 添加 `--adp` 标志到 `main()`
   - 根据 mode 选择调用 `ADPMCP` 或 `MVPMCP`
6. 创建 `.agent/adp-scope.json`（包含所有模块的完整 slideIds）
7. 更新 4 个模块的 `slides.json`，补充缺失的 slides
8. 创建 4 个逐字稿文件
9. 验证：`verify_course.py` ✅，`npm run build` ✅
10. 提交：`bb36efa`（快照）+ `d0e322c`（feat: ADP）+ `ed1d057`（docs: STATE/handoff）
11. 推送到远程：✅

### ADP 使用方式
```powershell
# MVP 模式（默认，只生成 p00/p01）
python .agent\flow_engine.py --mode production --scope module --module Module_01

# ADP 完整生产模式（生成所有 slides）
python .agent\flow_engine.py --mode production --adp --scope module --module Module_01
```

### 验证结果
- `verify_course.py` ✅ 通过
- `npm run build` ✅ 通过
- `flow_engine.py` 语法检查 ✅ 通过

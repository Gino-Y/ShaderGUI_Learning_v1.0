# 2026-05-03 工作日志

## 当前状态汇报

- 用户要求：汇报本工程当前状态。
- 判断：这是状态审查请求，不改变流程、产物契约、节点职责、验证标准、目录结构或交付定义。
- DAG 无需更新。
- 审查结论：当前验证脚本通过，本地页面 `/module/Module_01/slide/p01` 返回 200。
- v0 已进入 DAG：存在 `V0_READY` 与 `V0_PROTOTYPE_READY`，`.agent/v0/Module_01/react-prototype.json` 已生成。
- 当前 MVP scope 文件仍写 `p00/p01`，但当前 `slides.json` 实际包含 `p00/p01/p02` 三页，存在范围漂移。
- 根目录存在 `.cursor/rules/quiz-page-terminology.mdc`，违反 AI 资产统一维护在 `.agent/` 的规则。

## MVP scope 收敛

- 用户明确：当前 MVP scope 继续坚持 `p00/p01`。
- 处理：将 `MVPMCP.DEFAULT_MVP_SLIDE_IDS` 从 `p00/p01/p02` 收敛为 `p00/p01`，并要求输出不得越过 `.agent/mvp-scope.json` 生成 `p02` 等历史测试页。
- DAG：已更新 `docs/Skill_Chain_DAG.md` 的内容生产型 MVP 范围，当前 MVP 默认仅覆盖 `Module_01:p00,p01`。
- 规则：已更新 `.agent/rules.md`，把 scope 约束和 `.cursor` 中回流的做题页术语规则统一维护到 `.agent/`。
- 执行：重新运行 `python .agent\flow_engine.py --mode production --scope module --module Module_01 --basedir . --max-retries 5`。
- 结果：清理与 MVP 基础产物生成成功，manifest 显示 `slides=2`，`slides.json` 仅包含 `p00/p01`，`course.json.slideCount=2`。
- 阻断：`V0MCP.generate_react_prototype` 返回 v0 `HTTP 402 payment_required_error`，账号额度用尽，未生成 `.agent/v0/Module_01/react-prototype.json`；因此后续 `DesignMCP`、`VoiceMCP`、`StitchMCP`、build 与 audit 未进入。
- 自检：`scripts/verify_course.py` 按预期失败，缺少 v0 prototype、design contract、stitch manifest、p00/p01 音频和字幕；这是 v0 节点阻断后的连锁结果，不是 scope 漂移。

## v0 额度排查

- 用户要求：引导解决 v0 问题。
- 判断：这是接入阻断排查，不改变 DAG 节点、边、产物契约或 scope。
- DAG 无需更新。
- 验证：使用 `.env` 中 `V0_API_KEY` 调用 `GET https://api.v0.dev/v1/user/billing` 成功。
- 结果：`billingType=token`，`plan=v0-level0`，月度额度 `total=5`，当前 `remaining=0`，`onDemand.balance=0`。
- 结论：本地 key 与 API 接入正常，阻断原因是 v0 账号可用生成额度为 0。

## v0 升级后 MVP 验证

- 用户反馈：v0 已升级。
- 判断：这是对既有 DAG 的阻断恢复和重新验证，不改变节点、边、scope、目录结构或交付定义。
- DAG 无需更新。
- billing 验证：`plan=v0-team`，当前可用余额约 `36.222147`，v0 生成额度已恢复。
- 执行：重新运行 `python .agent\flow_engine.py --mode production --scope module --module Module_01 --basedir . --max-retries 5`。
- 结果：全链路到 `DEPLOY_READY`，v0 chat 创建成功，chat id 为 `rnIXigERm82`。
- scope 自检：`slides.json`、`design-contract.json`、`stitch-manifest.json` 均只包含 `p00/p01`，`course.json.slideCount=2`。
- 验证：`python scripts\verify_course.py` 通过；本地 `http://localhost:5173/module/Module_01/slide/p00` 返回 200。

## 本地服务恢复

- 用户反馈：in-app browser 访问 `localhost:5173` 被拒绝连接。
- 判断：这是本地 dev server 进程可用性问题，不改变 DAG 节点、边、scope、产物契约、目录结构或交付定义。
- DAG 无需更新。
- 处理：重新以独立后台进程启动 `CourseApp` 的 Vite dev server，日志写入 `.agent/memory/vite-5173.out.log` 与 `.agent/memory/vite-5173.err.log`。
- 验证：`0.0.0.0:5173` 已监听，`http://127.0.0.1:5173/module/Module_01/slide/p00` 返回 200。

## 做题页体验收敛

- 用户反馈：做题页默认只留当前题卡，提交后依次展现下一题，最后展示成绩页并允许重新挑战；移除其他无关内容，并解释故事板实时互动指导、设计运行时指导为何出现。
- 判断：这改变了做题页运行时体验、验证规则和生成模板，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md` 的做题页契约，禁止题库表、全量题卡流、故事板指导、设计运行时指导等内部或无关内容回流到做题页。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`CourseApp/src/views/QuizView.vue` 改为单题卡流程，提交后自动推进，结束后显示成绩页与“重新挑战”；移除 `storyboard-contract.json`、`design-contract.json` 导入和运行时展示。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 的 QuizView 生成模板和 `scripts/verify_course.py` 的必需/禁止标记。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/module/Module_01/quiz` 返回 200。

## 错题答案展示优化

- 用户反馈：成绩页需要更好地展示错题答案。
- 判断：这改变做题页成绩页交互与验收规则，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，要求成绩页展示学习者选择、正确答案、漏选项、误选项，并在选项列表中标记正确答案和学习者选择。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`QuizView.vue` 的成绩页新增错题复盘区，存储 `selected/expected`，并通过 `selectedOptionLabels`、`correctOptionLabels`、`missedOptionLabels`、`wrongOptionLabels`、`optionReviewClass` 展示答案差异。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 生成模板与 `scripts/verify_course.py` 验证标记。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/module/Module_01/quiz` 返回 200。

## 错题双描述移除

- 用户反馈：成绩页已在选项行中标记“正确答案/你的选择”，下方无需再次完整说明“你的选择/正确答案/漏选/误选”。
- 判断：这改变做题页成绩页交互与验收规则，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，要求成绩页保留选项级标记，下方只保留简短错因提示，禁止答案文本双描述。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`QuizView.vue` 移除 `selectedOptionLabels`、`correctOptionLabels`、`missedOptionLabels`、`wrongOptionLabels` 及对应文本块，改为 `reviewHint`、`missedOptionCount`、`wrongOptionCount` 的轻量提示。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 生成模板与 `scripts/verify_course.py` 必需/禁止标记。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/module/Module_01/quiz` 返回 200。

## 继续练习入口

- 用户反馈：在成绩页“重新挑战”同级增加“继续练习”按钮及功能，可在题库中继续随机抽样测验。
- 判断：这改变做题页成绩页交互与验收规则，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，要求成绩页提供“继续练习”，从题库随机抽样进入下一轮测验。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`QuizView.vue` 新增 `继续练习` 同级按钮和 `continuePractice`，从当前模块题库随机抽样后重置答案、结果和选项顺序。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 生成模板与 `scripts/verify_course.py` 验证标记。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/module/Module_01/quiz` 返回 200。

## 做题页返回策略

- 用户反馈：设计做题页返回策略，既可以回到当前课程，也可以去下一课，还可以回到菜单栏。
- 判断：这改变做题页成绩页交互与验收规则，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，要求成绩页提供三路返回策略，并在下一课不存在时显示不可用状态。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`QuizView.vue` 成绩页新增“回到当前课程”“下一课”“回到菜单”三个学习去向；当前课程指向本模块第一课，下一课指向本模块第二课，菜单指向本模块目录。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 生成模板与 `scripts/verify_course.py` 验证标记。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/module/Module_01/quiz` 返回 200。

## 单层菜单改造

- 用户反馈：修改菜单页，取消两级菜单，首页与模块页同时展示。
- 判断：这改变 SPA 路由结构、菜单体验与验证规则，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，要求 `/` 作为统一菜单页展示课程首页、模块信息、课时入口和做题页入口；`/module/:moduleId` 只作为兼容入口重定向到 `/`。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：`CourseHome.vue` 直接展示模块、课时入口和做题页入口；`router/index.js` 将 `/module/:moduleId` 改为 `redirect: "/"`；`QuizView.vue` 的“回到菜单”改为 `/`。
- 防回归：同步更新 `.agent/mcp_servers/mvp_mcp.py` 生成模板与 `scripts/verify_course.py` 验证标记，禁止 `CourseHome.vue` 再链接二级模块菜单，禁止 `ModuleHome` 作为路由组件。
- 验证：`python scripts\verify_course.py` 通过；`npm run build` 通过；`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`http://localhost:5173/` 返回 200；`http://localhost:5173/module/Module_01` 兼容返回 200 并由 SPA 重定向处理。

## Cursor Canvas 技能库迁移

- 用户要求：执行迁移，将 Cursor canvas 中的方案技能库统一迁移到 `.agent`，并更新 DAG 与规则。
- 参考源：`C:\Users\Administrator\.cursor\projects\d-Works-Web-ShaderGUI-Learning-v1-0\canvases\web-interactive-content-skill-brief.canvas.tsx`。
- 判断：这改变 Skill Registry、Skill Router、子 Skill 执行器和规则说明，必须更新 DAG。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，明确 `Skill Registry + Skill Router + 子 Skill 执行器` 结构，并登记 `.agent/skills/registry.json`。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`。
- 处理：补齐 `.agent/skills/interactive-article-skill/`、`.agent/skills/parameter-playground-skill/`、`.agent/skills/creative-workbench-skill/`、`.agent/skills/animated-lesson-skill/`，并增强 `web-interactive-content-builder` 与 `skill-router`。
- 处理：新增 `.agent/skills/registry.json` 作为机器可读 Skill Registry。
- 验证：registry 中 6 个子 Skill 均存在；项目根目录 `.cursor` 与 `.workbuddy` 均不存在。

## 交互必要性闸门

- 用户要求：将 `interaction-necessity-gate` 写入 DAG、规则、Skill 和验证脚本，但暂不新增 `p02`。
- 判断：这改变探索页进入课程的 DAG 门禁、Skill 职责和验证规则，必须更新 DAG；不改变当前 MVP scope。
- DAG：已更新 `docs/Skill_Chain_DAG.md`，新增 `S2G: interaction-necessity-gate`，位于模块/阶段拆分与页面结构规划之间。
- 规则：已同步更新 `.agent/rules.md` 与 `.agent/SKILL.md`，明确不得为了设计而设计、为了增加功能而增加探索页。
- Skill：已更新 `web-interactive-content-builder`、`skill-router`、`explorable-mini-skill`，要求 interactive slide 先生成 `.agent/interactive-content/<module-id>/<slide-id>/necessity-gate.json` 且 `decision=insert`。
- 防回归：已更新 `scripts/verify_course.py` 与 `.agent/mcp_servers/mvp_mcp.py` 的验证模板；未来 `kind=interactive` 的 slide 必须有通过闸门的 gate 文件。
- 验证：`python -m py_compile .agent\mcp_servers\mvp_mcp.py scripts\verify_course.py` 通过；`python scripts\verify_course.py` 通过。
- Scope：当前仍坚持 `Module_01:p00,p01`，未新增 `p02`。

## Cursor 接手记录

1. **交接协议落地**
   - 目标：将用户提供的 Cursor / Codex 交接规则落地为项目内状态、交接和记忆机制。
   - 修改：创建 `.agent/STATE.md` 与 `.agent/handoff/CURSOR_HANDOFF.md`；更新 `docs/Skill_Chain_DAG.md`、`.agent/rules.md`、`.agent/SKILL.md`、`scripts/verify_course.py`、`.agent/mcp_servers/mvp_mcp.py`，把交接文件纳入验证。
   - 验证：`python scripts/verify_course.py` 通过；`npm --prefix CourseApp run build` 通过；`python -m py_compile scripts/verify_course.py .agent/mcp_servers/mvp_mcp.py` 通过。
   - 风险：完整生产 DAG 本轮尚未执行；Windows 下 `node_modules` 清理可能遇到 EPERM；根目录历史 `nul` 条目可能干扰广域扫描或 git staging。
   - 下一步：Codex 恢复后先读取交接六件套，再根据最新用户目标继续；如需完整生产回归，运行 `.agent/flow_engine.py` 生产链路。

2. **交接协议文档化**
   - 目标：将 Cursor / Codex 交接规则从分散规则收敛为权威协议文档。
   - 修改：新增 `.agent/handoff/HANDOFF_PROTOCOL.md`；更新 `docs/Skill_Chain_DAG.md`、`.agent/rules.md`、`.agent/SKILL.md`、`scripts/verify_course.py`、`.agent/mcp_servers/mvp_mcp.py`；将 `CURSOR_HANDOFF.md` 改为本轮要求的中文交接格式。
   - 验证：`python scripts/verify_course.py` 通过；`npm --prefix CourseApp run build` 通过；`python -m py_compile scripts/verify_course.py .agent/mcp_servers/mvp_mcp.py` 通过。
   - 风险：完整生产 DAG 本轮尚未执行；根目录历史 `nul` 条目仍可能干扰 Windows 下广域扫描或 git staging。
   - 下一步：Codex 恢复后先读取交接六件套；如需完整生产回归，再运行 `.agent/flow_engine.py` 生产链路。

## 云端开发与监控接入

- 用户要求执行：代码托管、云端开发、自动验证、预览部署、监控、变更回流。
- 判断：这是工程流程契约变更，不是局部前端实现任务；无需再确认是否为局部任务，必须更新 rule 与 DAG。
- 已新增 `.github/workflows/cloud-validate.yml`：GitHub Actions 执行 `npm ci`、`npm run build`、`python scripts/verify_course.py`、本地 preview HTTP 监控，并支持 schedule 与手动回流报告。
- 已新增 `.devcontainer/devcontainer.json`：GitHub Codespaces / VS Code Remote 开发环境。
- 已新增 `.gitpod.yml`：Gitpod 云端开发入口。
- 已新增 `vercel.json`、`netlify.toml`、`wrangler.toml`：Vercel / Netlify / Cloudflare Pages 预览部署配置。
- 已新增 `scripts/cloud_monitor.py`：检查首页、幻灯片路由、音频资源与字幕资源，并生成 Markdown 报告。
- 已新增 `docs/Cloud_Operations.md`：云端接入与运行说明。
- 已更新 `.agent/rules.md`：新增“云端开发与监控规则”。
- 已更新 `docs/Skill_Chain_DAG.md`：新增 S15-S20 云端运营层节点。

## 音频生成质量修复

- 用户反馈：文生音频方法是否改变，当前音频质量很差。
- 结论：方法确实退化了。历史上使用 `edge-tts` 生成真实讲解 MP3，当前 `scripts/generate_audio.py` 和 `MVPMCP` 模板退回到 Windows `System.Speech` 生成 WAV，音质明显较差。
- 已修复 `scripts/generate_audio.py`：默认使用 `edge_tts` 与 `zh-CN-XiaoxiaoNeural` 生成高质量 MP3，同时继续生成字幕 JSON。
- 已修复 `.agent/mcp_servers/mvp_mcp.py`：MVP 自举模板改为生成 `/audio/Module_XX/pNN.mp3`，避免后续再回退到 WAV。
- 已更新 `CourseApp/src/data/slides.json`：音频路径从 `.wav` 改回 `.mp3`。
- 已重新生成 `CourseApp/public/audio/Module_01/p00.mp3` 与 `p01.mp3`，并删除旧 `p00.wav` / `p01.wav`。
- 已修复本轮验证暴露的模板残留问题：`scripts/verify_course.py` 缩进、`SlideNav.vue`、`ModuleHome.vue`、`QuizView.vue` 的闭合标签/乱码文案。
- 已更新 `docs/Skill_Chain_DAG.md`：当前 MVP 音频产物必须优先使用高质量神经 TTS MP3，不得使用 Windows `System.Speech` WAV 作为成功交付。
- 已更新 `.agent/rules.md`：新增“音频质量规则”。
- 验证通过：`python -m py_compile ...`、`npm run build`、`python scripts/verify_course.py`、`python scripts/cloud_monitor.py`。


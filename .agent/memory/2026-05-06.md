# 2026-05-06 Memory Log

## clean_text H1 标题泄露修复
- `generate_audio.py` clean_text() 新增正则移除 H1 文档标题行（匹配 `—— 逐字稿`）
- 全部 4 模块 12 个音频 + 12 个字幕重新生成
- DAG 不受影响（仅文本预处理逻辑变更）

## 新增规则
- `[DUAL_COMPLIANCE]` 双规定义录入 rules.md
- `[NO_CONTEXT_INFERENCE]` 禁止上下文推测执行流程

## 交接层检查
- 仅检查 `.agent` 与 handoff，不展开 CourseApp 产物内容。
- 发现 `.workbuddy/AI_TASK_BOOTSTRAP_POINTER.md` 与 `.workbuddy/memory/2026-05-05.md` 残留；对应 ADP 信息已在 `.agent/handoff/` 与 `.agent/memory/` 中记录。
- 已删除 `.workbuddy/` 平台私有目录，并将 latest 平台违规报告更新为 resolved。

## 2026-05-06 flow_engine ADP CLI guard
- Checked flow engine robustness without expanding CourseApp product content.
- Found and fixed a mode-boundary gap: `--mode test --stage mvp --adp` now uses `ADPMCP._clean_adp_products()` instead of MVP `clear_stage_outputs("mvp")`.
- Verification: `python -m py_compile` for flow/MCP files passed; `python .agent\platform_violation_guard.py --basedir .` passed.
- Product DAG/build was not rerun in this check to preserve the user's token-saving constraint.

## 代码示例 + 做题页 + 探索页数据填充
- 4 个模块 slides.json 添加 codeBlocks（12 个代码块，从 doc/*.md 提取）
- Module_01 p01 无代码栅栏，手写最小 OnGUI + CustomEditor 示例
- Module_02/03/04 quizzes.json 从旧扁平格式转换为嵌套 questions[] 格式
- Module_02/03/04 explorations.json 添加探索记录，slides.json 添加 explore 入口
- DAG 不受影响
- 待办：创建 SmartUILinkageLab、ModularAssemblyLab、RenderStatePlayground 组件

## 2026-05-06 ADP imperfection root cause
- Root cause found: ADPMCP writes full `slides.json` across all modules, but StoryboardMCP, DesignMCP, and StitchMCP still filter to the current module and overwrite one global contract file.
- Current data confirms only `Module_04` is covered by `storyboard-contract.json`, `design-contract.json`, and `stitch-manifest.json`; Module_01~03 slides are missing from those contracts.
- `scripts/verify_course.py` previously passed because it did not enforce full slide coverage for storyboard/design/stitch contracts.
- Added coverage checks to both `scripts/verify_course.py` and `.agent/templates/scripts/verify_course.py`; verification now correctly fails until ADP contract generation is made full-coverage or per-module contract files are introduced.

# 2026-05-05 Daily Memory

## Session 1: ADPMCP 平行 DAG 节点创建

### 用户请求
"建一个 adp_mcp 跟 mvp_mcp 平级的 DAG 节点"

### 完成工作
- 新建 `.agent/mcp_servers/adp_mcp.py`（平行节点，不修改 `mvp_mcp.py`）
- 更新 `flow_engine.py` 支持 `--adp` 标志
- 创建 `.agent/adp-scope.json`（完整 slideIds 配置）
- 更新 4 个模块的 `slides.json`，补充缺失的 slides
- 新增 4 个逐字稿文件
- Git 提交：`bb36efa`（快照）+ `d0e322c`（feat: ADP）

### 验证
- `verify_course.py` ✅
- `npm run build` ✅
- `flow_engine.py` 语法检查 ✅

---

## Session 2: ADPMCP 清理配置独立化

### 用户问题
"运行 ADP 模式会先清理产物吗？"

### 根因
`adp_mcp.py` 复用了 `MVPMCP` 的清理函数 `_clean_mvp_products()`，读取的是 `mvp-execution-scope.json`，语义不对。

### 修复
- 新建 `.agent/adp-execution-scope.json`（ADP 独立清理范围）
- 新建 `docs/ADP_Execution_Contract.md`（ADP 执行契约）
- 修改 `.agent/mcp_servers/adp_mcp.py`：
  - `_clean_mvp_products` → `_clean_adp_products`
  - 读取 `adp-execution-scope.json`（不再复用 MVP 的）
  - 检查 `docs/ADP_Execution_Contract.md`（不再检查 MVP 的）
- ADPMCP 现在有自己的清理配置，不再依赖 MVP 的 scope 文件

### Git 提交
- `3308675`（快照：Pre-ADP scope fix）
- `c945fdc`（feat(ADP): 独立化 ADPMCP 清理配置）

### 验证
- `verify_course.py` ✅
- `adp_mcp.py` 语法检查 ✅
- 推送远程 ✅

---

## 关键决策

1. **架构决策**：ADPMCP 作为平行 DAG 节点（非修改 MVPMCP）
   - MVPMCP：MVP 模式，读取 `mvp-scope.json`，只生成 p00/p01
   - ADPMCP：ADP 模式（--adp 标志），读取 `adp-scope.json`，生成所有 slides

2. **Git 规范**：严格遵循 rules.md
   - 先 `git restore` 恢复错误修改
   - 创建 Git 快照（snapshot commit）
   - 修改代码
   - 验证（verify_course.py + npm run build）
   - 提交（feat/docs 类型）
   - 推送到远程
   - 更新 STATE.md、CURSOR_HANDOFF.md、memory/2026-05-05.md

---

## 待办

- 测试 ADPMCP 完整生产流程：
  ```powershell
  python .agent\flow_engine.py --mode production --adp --scope module --module Module_01 --basedir . --max-retries 5
  ```

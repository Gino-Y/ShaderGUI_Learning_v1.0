
## 2026-05-05 Session 2

### SOUL.md 更新
- 执行方案A：将 `.agent/rules.md` 核心约束（GIT_SNAPSHOT_BEFORE_MODIFY、NO_DIRECT_EDIT_OUTPUT、STOP_DEV_SERVER_BEFORE_MVP 等）写入 `SOUL.md` 的 "Before Any Work" 段落
- 目的：每次会话必然能看到核心约束，不再依赖"是否读了 rules.md"

### 表演动画丢失问题（反复反馈的第 N 次）
**根因**：`PerformanceLayer.vue` 只处理 `type === 'decoration'` 和 `type === 'transition'`，不处理 `type === 'demo'`
- `storyboard-contract.json` p01 的 `performanceSpecs` 含有 `"type": "demo", "demo": "flow-path"`
- 但 `PerformanceLayer.vue` 没有导入 `FlowPathDemo.vue`，也没有对应渲染分支

**修复**：
1. 修改 `.agent/templates/course-app/src/components/PerformanceLayer.vue`：
   - 新增导入 `FlowPathDemo` from `./performances/FlowPathDemo.vue`
   - template 新增 `v-else-if="activePerfSpec.type === 'demo' && activePerfSpec.demo === 'flow-path'"` 分支
   - 传递 `:payload`、`:durationMs`、`:active` 三个 props
2. Git 快照：`3d0d657` Snapshot: Pre-MVP - Fix PerformanceLayer demo type support
3. 停止所有 node.exe 进程（开发服务器占用文件导致 MVP 清理失败）
4. 运行 MVP：`python .agent/flow_engine.py --mode production --scope module --module Module_01`
   - 结果：DEPLOY_READY ✓
5. 产物验证：
   - `CourseApp/src/components/PerformanceLayer.vue` 含 FlowPathDemo 分支 ✓
   - `storyboard-contract.json` p01 含 `demo` 类型 ✓
6. Git 提交推送：`5c417f7` fix: PerformanceLayer 支持 demo 类型（flow-path），MVP 重生成产物
7. 开发服务器启动：`http://localhost:5173/` 返回 200 ✓
8. `verify_course.py` 通过 ✓

**结论**：表演动画（FlowPathDemo）现在应该能正常渲染了。用户需在浏览器中测试 p01 的 perf-cue-02（flow-path demo）。

### 教训
- 每次会话开始必须读 `.agent/rules.md`（现在已写入 SOUL.md，但读原文仍然必要）
- `performanceSpecs` 的新 `type` 必须同步到 `PerformanceLayer.vue` 模板，否则 MVP 重生成后产物永远缺这块
- 这应写入 DAG 检查清单，防止再次漂移

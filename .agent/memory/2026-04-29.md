# 2026-04-29 工作日志

## ShaderGUI_Learning_v1.0 教学体系建立

### 完成内容

1. **整理完整教学文档** → `docs/ShaderGUI_Teaching_Plan.md`
   - 四大模块（解构/重构/进化/治理）全部 12 张 PPT 要点与讲稿
   - 含代码示例、反例对比、工程金句

2. **生成项目级 SKILL** → `.agent/SKILL.md`
   - 涵盖四大模块的核心代码规范与模板
   - AI 辅助开发 SOP（追问 → 审查 → 费曼验收）
   - 常用 API 速查手册

3. **生成项目级 rules** → `.agent/rules.md`
   - P0 禁止行为 5 条（含原因 + 替代方案）
   - P1 警告行为 4 条
   - P2 最佳实践建议
   - 文件/命名约定
   - 记忆检查点 SOP

### 待推进

- [ ] M_Outline_Fitting_h01 实战验收代码实现
- [ ] PPT 文件制作（pptxgenjs 渲染）

### 新增决策

4. **搭建课程生产 Skill 链 DAG** → `docs/Skill_Chain_DAG.md`
   - 参考 `D:/Works/Web/Git_learning` 的 `slide-course-builder` 链路
   - 在“HTML PPT → 逐字稿”之后新增：逐字稿口播自检、讲解音频生成、`slides.json` 绑定、音频接入 HTML PPT
   - 明确最终目标为可播放、可验证、可交付的 DAG，而不是单线文本生成流程
   - 更新 `.agent/SKILL.md` 到 `1.1.0`，加入课程生产 Skill 链入口
   - 更新 `.agent/rules.md` 到 `1.0.2`，记忆检查点新增 `docs/Skill_Chain_DAG.md`

5. **课程前端工作流改为 Vue SPA**
   - 最终前端形态确定为 Vite + Vue 3 + Vue Router + Tailwind CSS
   - HTML 版 PPT 不再以多份 `Module_XX-pNN.html` 静态页面交付，而是作为 Vue 路由中的幻灯片视图
   - 路由结构：`/`、`/module/:moduleId`、`/module/:moduleId/slide/:slideId`
   - 课程数据由 `CourseApp/src/data/course.json` 和 `CourseApp/src/data/slides.json` 驱动
   - 音频接入改为 Vue 播放器组件读取 manifest
   - 更新 `.agent/SKILL.md` 到 `1.2.0`
   - 更新 `.agent/rules.md` 到 `1.0.3`

6. **Vue SPA MVP 验证完成**
   - 创建 `CourseApp/` Vite + Vue 3 + Vue Router + Tailwind CSS 最小工程
   - 创建 `CourseContent/Module_01/` 与 `CourseApp/public/transcripts/Module_01/` 逐字稿样例
   - 创建 `CourseApp/src/data/course.json` 与 `CourseApp/src/data/slides.json`
   - 创建播放器、幻灯片画布、导航、逐字稿面板等最小组件
   - 使用 ffmpeg 生成 `public/audio/Module_01/p00.mp3` 与 `p01.mp3` 静音占位音频
   - 创建 `scripts/verify_course.py` 自动验证脚本
   - `npm run build` 已通过
   - `python scripts/verify_course.py` 已通过
   - `npm audit --audit-level=moderate` 已通过，0 vulnerabilities
   - Vite dev server 已启动：`http://localhost:5173/`
   - HTTP 检查已通过：`/` 与 `/module/Module_01/slide/p00` 均返回 200

7. **MVP 音频从占位升级为真实 TTS**
   - 新增 `scripts/generate_audio.py`
   - 使用 `edge-tts` 从 `CourseApp/public/transcripts/Module_01/*.md` 生成真实讲解 mp3
   - 替换 `CourseApp/public/audio/Module_01/p00.mp3` 与 `p01.mp3`
   - `p00.mp3` HTTP 检查通过：`/audio/Module_01/p00.mp3` 返回 200，约 232 KB
   - `scripts/verify_course.py` 增加音频占位检测，过小 mp3 会失败
   - `npm run build` 与 `python scripts/verify_course.py` 重新验证通过

8. **修复课程内容污染问题**
   - 问题：p00 幻灯片和逐字稿混入了“MVP / Vue / SPA / 工作流 / 验证”等课程生产工程说明
   - 决策：课程页面、逐字稿、音频只允许出现 ShaderGUI 教学内容；课程生产过程只能存在于 docs、rules、memory、scripts
   - 已替换 p00 第三个要点为“职责是组织参数、解释含义、同步状态”
   - 已重写 p00 逐字稿，删除所有工程验证描述
   - 已重新生成 `CourseApp/public/audio/Module_01/p00.mp3`
   - `scripts/verify_course.py` 新增课程内容禁词检查，拦截 MVP、Vue、SPA、工作流、构建、验证脚本、课程数据、播放器绑定、路由等工程词进入课程内容
   - `npm run build`、`python scripts/verify_course.py`、p00 页面 HTTP、p00 音频 HTTP 均重新通过

9. **AI 资产统一迁移至 `.agent`**
   - 决策：项目 AI 规则、Skill、记忆、工作流说明统一维护在 `.agent/`
   - 目的：与 WorkBuddy、Cursor 等具体 AI 平台解耦
   - 已迁移 `.workbuddy/memory/MEMORY.md` → `.agent/memory/MEMORY.md`
   - 已迁移 `.workbuddy/memory/2026-04-29.md` → `.agent/memory/2026-04-29.md`
   - 已将 `docs/Skill_Chain_DAG.md` 中拟落地 Skill 路径改为 `.agent/skills/<skill-name>/SKILL.md`
   - 已在 `.agent/rules.md` 烙印平台解耦规则，禁止新增 `.workbuddy/`、`.cursor/` 等平台专属目录

10. **技能链理解修正：从文档节点升级为可执行运行时**
   - 参考 `D:/Works/Web/Git_learning_v4.0/.agent`
   - 认识修正：Skill 链不是节点名称清单，而是 `FlowState + CorePipeline + MCP 节点模块 + run_guard` 的可执行状态机
   - 新增 `.agent/flow_engine.py`
   - 新增 `.agent/run_guard.py`
   - 新增 `.agent/mcp_servers/course_mcp.py`
   - 新增 `.agent/mcp_servers/voice_mcp.py`
   - 新增 `.agent/mcp_servers/app_mcp.py`
   - 更新 `docs/Skill_Chain_DAG.md`，记录已落地运行时结构、状态流转和旧理解修正
   - 修复 Windows 子进程输出编码问题，统一使用 UTF-8 + replace
   - `python .agent/flow_engine.py --mode production --scope module --module Module_01 --basedir .` 已跑通，最终状态 `COMPLETED`

11. **清空旧产物后重新 MVP 验证**
   - 删除旧 `CourseApp/`、`CourseContent/`、`scripts/`
   - 保留 `.agent/` 与 `docs/` 作为规则、技能链和源文档
   - 重新创建干净 `CourseApp/` Vue SPA MVP
   - 重新创建干净 `CourseContent/Module_01/` 与 `scripts/`
   - 课程内容只保留 ShaderGUI 教学内容，不包含 MVP、Vue、SPA、工作流、构建、验证脚本、课程数据、播放器绑定、路由等工程说明
   - `npm install` 完成，0 vulnerabilities
   - `python .agent/flow_engine.py --mode production --scope module --module Module_01 --basedir .` 完成，最终状态 `COMPLETED`
   - `python scripts/verify_course.py` 通过
   - `npm audit --audit-level=moderate` 通过，0 vulnerabilities
   - p00 页面 HTTP 200，p00 音频 HTTP 200，音频约 321 KB

12. **逐字稿展示改为音频事件字幕**
   - 决策：逐字稿是生产材料和 TTS 来源，不应作为 PPT 面板直接展示
   - PPT 播放态只展示由音频时间事件驱动的字幕
   - 删除 `CourseApp/src/components/TranscriptPanel.vue`
   - 新增 `CourseApp/src/components/SubtitleOverlay.vue`
   - `scripts/generate_audio.py` 现在同时生成 `/subtitles/Module_XX/pNN.json`
   - `CourseApp/src/data/slides.json` 增加 `subtitles` 字段
   - `SlideNav.vue` 监听 `audio timeupdate` 并向 `CoursePlayer.vue` 派发当前字幕
   - `scripts/verify_course.py` 增加字幕文件存在性、事件 schema 和禁止 TranscriptPanel 回归检查
   - `.agent/flow_engine.py` 全链路重新跑通，最终状态 `COMPLETED`

13. **反馈必须更新 DAG 规则落地**
   - 决策：每次用户反馈后，必须审查并更新 `docs/Skill_Chain_DAG.md`
   - 若反馈改变流程、产物契约、节点职责、验证标准、目录结构或交付定义，必须同步更新 DAG
   - 若反馈不影响 DAG，也必须在工作日志说明“不影响 DAG”的判断
   - 已写入 `.agent/rules.md` 的“反馈驱动 DAG 更新规则”
   - `.agent/rules.md` 版本更新为 `1.2.0`
   - 已补齐 `docs/Skill_Chain_DAG.md` 中“逐字稿不得展示为 PPT 面板，应生成音频事件字幕”的产物契约
   - `.agent/flow_engine.py` 全链路重新跑通，最终状态 `COMPLETED`

14. **引入 StitchMCP**
   - 用户反馈：在工作流中引入 stitch mpc
   - 按参考工程语义修正为 `StitchMCP`
   - 新增 `.agent/mcp_servers/stitch_mcp.py`
   - `StitchMCP` 职责：在音频与字幕事件生成之后，检查并缝合 `slides.json`、音频、字幕、逐字稿和 Vue 播放器组件
   - 新增产物：`CourseApp/src/data/stitch-manifest.json`
   - `.agent/flow_engine.py` 新增状态 `STITCHED`，流程变为 `AUDIO_READY -> STITCHED -> COURSE_VERIFIED`
   - `scripts/verify_course.py` 增加 stitch manifest 覆盖性检查
   - `docs/Skill_Chain_DAG.md` 已同步更新 S10 与状态流转
   - `.agent/flow_engine.py` 全链路重新跑通，最终状态 `COMPLETED`

15. **StitchMCP 职责解释，不改变 DAG**
   - 用户询问：当前链路中 Stitch 起到什么作用
   - 判断：这是对既有 DAG 节点职责的解释，不改变流程、产物契约、节点职责或验证标准
   - DAG 无需更新

16. **Stitch 概念解释，不改变 DAG**
   - 用户询问：什么是 Stitch
   - 判断：这是对既有概念的解释，不改变流程、产物契约、节点职责或验证标准
   - DAG 无需更新

17. **Stitch 术语澄清，不改变当前 DAG**
   - 用户澄清：前面提到的 stitch 指的是 Stitch AI，不是当前实现中的 `StitchMCP` 抽象
   - 当前决策：现有 DAG 暂时不用改
   - 后续若正式引入 Stitch AI，需要重新定义其输入、输出、调用边界、产物契约和验证标准，再更新 DAG

18. **正式引入 Stitch AI 并完成 MVP 验证**
   - 用户要求：引入 Stitch AI，并验证
   - DAG 已更新：新增 `STITCH_AI_READY` 状态与 `S3A stitch-ai-design-handoff` 节点
   - 新增 `.agent/mcp_servers/stitch_ai_mcp.py`
   - 新增产物：`CourseApp/src/data/stitch-ai-design.json`
   - 新增产物：`.agent/stitch_ai/Module_01/stitch-ai-brief.md`
   - 当前没有保存 Stitch AI 在线服务凭据，因此采用 `stitch-ai-local-spec` provider，先完成可替换的本地设计契约与 handoff brief
   - 当前 Codex 环境未发现可调用的 Stitch AI MCP 工具，因此本次未执行在线 Stitch AI 调用
   - `scripts/verify_course.py` 已加入 Stitch AI 契约校验：provider、状态、slide 覆盖、visualPrompt、contentConstraints
   - `python .agent/flow_engine.py --mode production --scope module --module Module_01 --basedir .` 已通过，最终状态 `COMPLETED`
   - `python scripts/verify_course.py` 已通过

19. **MVP 产物边界修正：scripts / Vue 源码 / 课程源稿也必须可删除重生**
   - 用户反馈：`mp3`、`scripts/`、Vue 源码和课程源稿也是产物，需要删除，从新生成
   - DAG 已更新：新增 `MVP_PRODUCTS_READY` 状态与 `S0A mvp-product-generator` 节点
   - 新增 `.agent/mcp_servers/mvp_mcp.py`
   - `CourseApp/`、`CourseContent/`、`scripts/`、音频、字幕、构建目录、Stitch AI handoff 均定义为可再生产物
   - `.agent/flow_engine.py` 已接入 `MVPMCP.generate_products`，可从 `.agent + docs` 自举生成 MVP 产物后继续完整 DAG
   - 首次自举暴露缺口：运行时读取 `CourseApp/public/transcripts/`，仅生成 `CourseContent/` 不足以进入音频节点
   - 已修正 `MVPMCP.generate_products`：逐字稿同时生成到 `CourseContent/Module_XX/doc/` 与 `CourseApp/public/transcripts/Module_XX/`

20. **当前工程状态简报请求，不改变 DAG**
   - 用户要求：分析当前工程状态，输出一份简报供预览
   - 判断：这是状态审查与汇报请求，不改变流程、产物契约、节点职责、验证标准、目录结构或交付定义
   - DAG 无需更新
   - 审查发现：当前工程存在 `.workbuddy/`，违反 `.agent` 统一维护规则；`scripts/verify_course.py` 要求 `design-contract.json`，但当前产物缺失，验证失败

21. **规则违规修复：迁移并移除 `.workbuddy`，补齐设计契约**
   - 用户要求：解决 AI 资产必须统一维护在 `.agent/`，不能回流到 `.workbuddy/`；解决缺少 `design-contract.json`
   - 从 `.workbuddy/memory/` 归并的有效状态：Module_01 已扩展为 p00-p11 共 12 页；旧 `stitch-ai-design.json` 已泛化为 `design-contract.json`；`DesignMCP` 负责设计契约与双层循环自检
   - 判断：这是执行既有规则与补齐既有 DAG 产物，不新增流程定义；DAG 无需更新
   - 执行时发现 `.agent/run_guard.py` 存在缩进语法错误，已修复以恢复 flow_engine 启动能力
   - 执行时发现 `generate_audio.py` 模板生成 `00.mp3/00.json`，而 manifest 要求 `p00.mp3/p00.json`；已修正 `.agent/mcp_servers/mvp_mcp.py` 模板
   - 验证阶段发现模板内容回归：`C# ` 被 Markdown 残留规则误判，p06 标题含禁词“构建”；已修正为 `C Sharp` 与“组合材质面板”
   - 已删除 `.workbuddy/`，根目录未发现 `.workbuddy` / `.cursor`
   - 已重新执行完整 DAG，最终状态 `DEPLOY_READY`
   - 已确认 `CourseApp/src/data/design-contract.json` 存在，`python scripts/verify_course.py` 通过，本地路由 `http://localhost:5173/module/Module_01/slide/p01` 返回 200

22. **v0 API Key 接入指导，不改变 DAG**
   - 用户要求：引导接入 v0 API Key
   - 判断：这是本机/IDE 凭据配置指导，不改变当前课程生产 DAG、产物契约、节点职责或验证标准
   - 安全约束：不得把 `V0_API_KEY` 写入仓库、`.agent/`、docs、scripts 或任何可提交文件
   - DAG 无需更新

23. **创建本地 `.env` 承载 v0 API Key，不改变 DAG**
   - 用户要求：写入 `.env`
   - 已创建根目录 `.env`，仅包含 `V0_API_KEY=` 占位，不包含真实密钥
   - 已更新 `.gitignore`，忽略 `.env` 与 `.env.*`，保留 `.env.example` 可提交
   - 判断：这是本地凭据承载方式，不改变当前课程生产 DAG、产物契约、节点职责或验证标准
   - DAG 无需更新

24. **验证 v0 API Key，不改变 DAG**
   - 用户要求：已填写 `.env`，请验证
   - 已确认 `.env` 存在，`V0_API_KEY` 非空且不是占位符
   - 已确认 `.gitignore` 包含 `.env` / `.env.*` / `!.env.example`
   - 已使用只读接口 `GET https://api.v0.dev/v1/rate-limits` 验证认证，返回 200
   - 未打印、复制或写入真实密钥
   - 判断：这是本地凭据验证，不改变当前课程生产 DAG、产物契约、节点职责或验证标准
   - DAG 无需更新

25. **v0 接入验证型 MVP：清理全部产物并坚持两页**
   - 用户要求：执行 MVP（一并移除所有产物），且坚持只生产两页，目的是验证 v0 接入是否成功
   - DAG 已更新：新增 `V0_READY` 状态与 `S0V v0-api-key-probe` 节点
   - 新增 `.agent/mcp_servers/v0_mcp.py`，只调用 `GET https://api.v0.dev/v1/rate-limits`，不创建 v0 chat，不消耗生成额度
   - `MVPMCP.generate_products` 已加硬约束：v0 接入验证型 MVP 只生成 `p00/p01`
   - `run_guard.clear_stage_outputs(..., "mvp")` 已改为移除 `CourseApp/`、`CourseContent/`、`scripts/` 与 `.agent/design/Module_XX/`，保留 `.env`
   - 已执行完整 DAG：`V0_READY` 通过，最终状态 `DEPLOY_READY`
   - 已复核：`slides.json` 仅包含 `p00,p01`，`course.json.slideCount=2`
   - 已复核：`.env` 保留，`.workbuddy` / `.cursor` 不存在
   - 已复核：`python scripts/verify_course.py` 通过，本地路由 `http://localhost:5173/module/Module_01/slide/p01` 返回 200

26. **播放器连续播放与移动端手势契约，重新执行 MVP**
   - 用户要求：重新执行 MVP，验证首次打开自动播放、结束自动翻页并播放下一页、手动翻页自动播放、最后一页结束停止、底部可拖动进度条、移动端上下滑翻页与单击播放/暂停
   - DAG 已更新：S12 验证标准新增播放器交互契约，MVP 章节新增 `SlideNav.vue` / `CoursePlayer.vue` 职责
   - `MVPMCP.generate_products` 已更新播放器模板：自动播放、结束翻页、最后页停止、进度条 seek、松开续播、键盘左右翻页、触屏上下滑、主区域单击暂停/播放
   - `verify_course.py` 模板已增加播放器交互标记检查，防止重新 MVP 时交互能力丢失
   - 注意：浏览器首次有声自动播放可能被策略拦截；实现已主动 `audio.play()`，并支持首次单击恢复播放
   - Playwright 交互验证发现 Chromium 阻止首次有声自动播放；已加入静音预热自动播放策略，启动后立即解除静音，仍保留单击解锁兜底
   - 二次验证发现静音预热必须在首次 `play()` 前设置；已改为自动启动路径先 `muted` 再 `play()`，用户手势路径直接有声播放
   - 三次验证发现初始播放请求可能早于 audio ref 就绪，已修正 `requestInitialPlayback`：audio 未就绪时不消耗初始播放标记
   - 四次验证确认无手势解除静音会触发浏览器暂停；最终策略为静音自动启动时间轴，首次单击解除静音，随后单击恢复暂停/播放语义
   - 最终 Playwright 验证：除“首次无手势自动播放”外，进度条、单击交互、结束自动翻页、下一页自动播放、手动翻页自动播放、最后页停止、移动端上滑翻页均通过；首次无手势自动播放仍被 Chromium 策略拦截

27. **确认 v0 接入状态与画面未变化原因，不改变 DAG**
   - 用户要求：确认是否已成功接入 v0，并解释当前画面为何没有任何变化
   - 已复核：`.env` 存在，`V0_API_KEY` 非空且不是占位符
   - 已复核：`V0MCP.validate_api_key` 调用 `GET https://api.v0.dev/v1/rate-limits` 返回 success
   - 结论：当前接入只是 API Key 探测节点，不调用 v0 生成 UI，不消费生成额度，因此不会改变 Vue 画面
   - 判断：这是状态解释，不改变 DAG

29. **v0 从连通性接入升级为 UI 原型生成与 Vue 转译**
   - 用户要求：执行 `v0 生成设计/React 原型 -> 抽取布局、交互、视觉规则 -> 转译为 Vue 组件 -> 进入 MVP 验证`
   - 判断：该需求改变 DAG 流程、产物契约、节点职责和验证标准，不需要再确认是否为局部前端任务。
   - 已新增 `V0MCP.generate_react_prototype`：在故事板之后创建 v0 chat，输出 `.agent/v0/Module_01/react-prototype.json` 与 `react-prototype-brief.md`。
   - v0 chat 已创建：`hf1iW2Yj8Iy`，URL `https://v0.app/chat/hf1iW2Yj8Iy`。
   - 已更新 `.agent/flow_engine.py`：状态流新增 `V0_PROTOTYPE_READY`，链路为 `STORYBOARD_READY -> V0_PROTOTYPE_READY -> DESIGN_READY`。
   - 已更新 `DesignMCP`：provider 改为 `design-v0-translated-spec`，每页设计契约新增 `v0PrototypeRef`，承接 v0 chat、原型文件与设计规则。
   - 已将 v0 的暗色课程播放器原型规则转译到 `CourseApp/src/components/SlideCanvas.vue` 与 `MVPMCP` 模板，保持 Vue + Tailwind 运行时。
   - 已更新 `scripts/verify_course.py` 与 `MVPMCP` 验证模板，检查 v0 原型产物、设计契约 provider 与 `v0PrototypeRef`。
   - 已更新 `docs/Skill_Chain_DAG.md`、`.agent/SKILL.md`、`.agent/rules.md`。
   - 完整 DAG 已执行通过，最终状态 `DEPLOY_READY`；`verify_course.py`、`npm run build`、`npm audit` 均通过。
   - 清理守卫已调整：Windows 锁定的 `node_modules` 或 Vite 日志不再阻断 MVP 重生，源码与可再生产物仍会重写。

25. **播放器连续播放与移动端手势反馈，已更新 DAG**
   - 用户要求：首次打开自动播放音频；当前页播放结束自动翻页并播放下一页；手动翻页自动播放；最后一页结束停止；底部增加可拖动音频进度条；移动端上/下滑翻页，单击暂停/播放
   - 已修改 `CourseApp/src/components/SlideNav.vue`：自动播放、音频结束自动翻页、最后一页停止、进度条拖动定位、松开续播、字幕同步
   - 已修改 `CourseApp/src/components/CoursePlayer.vue`：主播放区域单击暂停/播放，上滑下一页，下滑上一页
   - 已修改 `CourseApp/src/styles/main.css`：触控手势约束与进度条触摸样式
   - `npm run build` 已通过
   - DAG 已同步更新：新增“播放器交互契约增量”，归入 `S10: stitch-runtime-integrator` 和 `S12: delivery-verifier`
   - 注意：浏览器首次有声自动播放可能被策略拦截，交付说明需提示首次点击解锁，后续连续播放应自动衔接

26. **局部前端实现任务确认规则，已更新 rule 与 DAG**
   - 用户要求：如有必要，即弹出是否为局部前端实现任务向用户确认；明确不需要就不必确认
   - 已更新 `.agent/rules.md`：新增“局部实现任务确认规则”，要求在可能误判为局部实现但可能影响契约时向用户确认
   - 已更新 `docs/Skill_Chain_DAG.md`：新增“反馈确认机制”，纳入反馈进入 Skill 链/DAG 的执行约束
   - 本次需求明确改变规则与反馈工作流，因此不需要先向用户确认是否为局部前端实现任务

27. **播放/暂停按钮文字改为图标，已按用户要求更新 DAG**
   - 用户要求：将播放器的“暂停/播放”文字改为图标
   - 初始判断：这是局部前端实现任务，仅改变按钮视觉呈现；用户随后明确要求将该操作更新给 DAG
   - 已修改 `CourseApp/src/components/SlideNav.vue`：播放/暂停按钮改为内联 SVG 图标，保留 `aria-label` 与 `title`
   - 已更新 `docs/Skill_Chain_DAG.md`：新增“播放器控件视觉契约”，归入 `S10: stitch-runtime-integrator` 与 `S12: delivery-verifier`
   - `npm run build` 已通过

28. **新增设计前故事板 DAG 节点，已更新 DAG / Skill / rules**
   - 用户需求：在设计环节前新增一个 DAG 节点，本质类似影视故事板，用于指导页面布局、配色、教学内容表达，并根据逐字稿字幕时间节点触发应有动效，对接未来 Web 动画模块。
   - 判断：该需求改变流程、产物契约、节点职责与验证标准，必须更新 DAG。
   - 已新增 `.agent/mcp_servers/storyboard_mcp.py`，产出 `CourseApp/src/data/storyboard-contract.json` 与 `.agent/storyboard/Module_XX/storyboard-brief.md`。
   - 已更新 `.agent/flow_engine.py`：新增 `STORYBOARD_READY` 状态，链路为 `MANIFEST_READY -> STORYBOARD_READY -> DESIGN_READY`。
   - 已更新 `.agent/mcp_servers/design_mcp.py`：设计契约承接 `storyboardRef`、`motionCues`、`animationHandoff`。
   - 已更新 `scripts/verify_course.py` 与 `MVPMCP` 内置验证模板：检查故事板契约、字幕触发器和设计承接关系。
   - 已更新 `docs/Skill_Chain_DAG.md`、`.agent/SKILL.md`、`.agent/rules.md`。

29. **MVP 范围从两页 v0 接入验证升级为内容生产验证**
   - 用户要求：把 MVP 范围从“两页 v0 接入验证型 MVP”调整为包含测试页/测试模块。
   - 判断：该需求改变 MVP 产物契约、验证标准和 DAG 范围定义，必须同步更新 DAG / rules / Skill / 验证模板。
   - 已移除 `MVPMCP._write_json` 中对 `course.json.slideCount=2` 与 `slides.json in {p00,p01}` 的硬截断。
   - 已移除逐字稿生成中只写 `p00/p01` 的过滤，当前 `Module_01` 的 `p00-p11` 都会双落点生成。
   - 已更新 `scripts/verify_course.py` 与 `MVPMCP` 内置验证模板：检查 `course.json.slideCount` 与 `slides.json` 实际页数一致，防止测试页被静默丢弃。
   - 已更新 `docs/Skill_Chain_DAG.md`、`.agent/rules.md`、`.agent/SKILL.md`，明确两页范围只适用于历史 v0 凭据验证，不适用于内容生产验证。

30. **修正 MVP 范围：从默认 12 页改为可配置 scope**
   - 用户追问：MVP 是否一定要重新生成完整 Module_01 的 p00-p11 共 12 页。
   - 判断：默认 12 页过重，MVP 应按验证目标收敛，完整模块只用于 full-module 回归。
   - 已更新 `MVPMCP.generate_products`：默认内容生产 MVP 为 `p00/p01/p02`，可通过 `.agent/mvp-scope.json` 的 `slideIds` 指定测试页。
   - `course.json.slideCount` 与 `slides.json` 会按当前 scope 写入，逐字稿也只生成 scope 内页面。
   - 已更新 `docs/Skill_Chain_DAG.md`、`.agent/rules.md`、`.agent/SKILL.md`，明确不得默认全量 12 页。

31. **播放器静音 fallback 修复与规则更新**
   - 用户反馈：默认只播放进度条和字幕，没有播放 mp3，要求循环自检直至交付并更新至 rule。
   - 判断：该反馈改变播放器验收标准和验证脚本，必须更新 DAG / rules / Skill。
   - 已修改 `CourseApp/src/components/SlideNav.vue` 与 `MVPMCP` 模板：移除 `autoplay muted` 和静音 fallback，不再用 `audio.value.muted = true` 推进时间轴。
   - 新逻辑：默认尝试有声播放；若浏览器拦截，则暂停等待用户点击；用户点击后以 `audio.value.muted = false` 调用播放。
   - 已更新 `scripts/verify_course.py` 与模板：检查有声播放标记，拦截静音自动播放 fallback。
   - 已更新 `.agent/rules.md`、`.agent/SKILL.md`、`docs/Skill_Chain_DAG.md`。

32. **做题页进入 DAG 契约**
   - 用户澄清：“测试交互页”指 quiz 页，即用户做测试题的页面；后续统一称为“做题页”。
   - 判断：该反馈改变课程运行时产物契约和验证标准，必须更新 DAG / rules / Skill。
   - 当前工程已有 `CourseApp/src/views/QuizView.vue`、`CourseApp/src/data/quizzes.json`、`/module/:moduleId/quiz` 路由和模块目录入口。
   - `scripts/verify_course.py` 已检查 quiz 文件、路由、题库结构、单选/多选、交换选项、提交判分和结果状态标记。
   - 已更新 `docs/Skill_Chain_DAG.md`、`.agent/rules.md`、`.agent/SKILL.md`，明确做题页不是 slide 测试页，也不是浏览器交互测试节点。

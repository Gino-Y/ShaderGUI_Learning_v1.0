# 2026-05-06 Memory Log

## Lab 组件创建 + ADP 全量通过
- 创建 SmartUILinkageLab.vue（Module_02 Toggle/Enum/联动控制面板）
- 创建 ModularAssemblyLab.vue（Module_03 BaseColor/RenderState/组装模块面板）
- 创建 RenderStatePlayground.vue（Module_04 透明/镂空/叠加渲染状态面板）
- 同步到 `.agent/templates/course-app/src/components/labs/`
- 更新 ExploreView.vue 模板注册 3 个新组件
- ADP dispatcher 全量 4 模块 DEPLOY_READY
- `verify_course.py` ✅，`npm run build` ✅（537ms）
- 提交 `f9d42e2`，快照 `e8e63ab`

## clean_text H1 标题泄露修复
- `generate_audio.py` clean_text() 新增正则移除 H1 文档标题行（匹配 `—— 逐字稿`）
- 全部 4 模块 12 个音频 + 12 个字幕重新生成
- DAG 不受影响（仅文本预处理逻辑变更）

## 新增规则
- `[DUAL_COMPLIANCE]` 双规定义录入 rules.md
- `[NO_CONTEXT_INFERENCE]` 禁止上下文推测执行流程

## 交接层检查
- 仅检查 `.agent` 与 handoff，不展开 CourseApp 产物内容。
- 发现 `.workbuddy/AI_TASK_BOOTSTRAP_POINTER.md` 与 `.workbuddy/memory/2026-05-05.md` 残留；对应 ADP 信息已在 `.agent/handoff/` 与 `.agent/memory/` 中记录。
- 已删除 `.workbuddy/` 平台私有目录，并将 latest 平台违规报告更新为 resolved。

## 2026-05-06 flow_engine ADP CLI guard
- Checked flow engine robustness without expanding CourseApp product content.
- Found and fixed a mode-boundary gap: `--mode test --stage mvp --adp` now uses `ADPMCP._clean_adp_products()` instead of MVP `clear_stage_outputs("mvp")`.
- Verification: `python -m py_compile` for flow/MCP files passed; `python .agent\platform_violation_guard.py --basedir .` passed.
- Product DAG/build was not rerun in this check to preserve the user's token-saving constraint.

## 代码示例 + 做题页 + 探索页数据填充
- 4 个模块 slides.json 添加 codeBlocks（12 个代码块，从 doc/*.md 提取）
- Module_01 p01 无代码栅栏，手写最小 OnGUI + CustomEditor 示例
- Module_02/03/04 quizzes.json 从旧扁平格式转换为嵌套 questions[] 格式
- Module_02/03/04 explorations.json 添加探索记录，slides.json 添加 explore 入口
- DAG 不受影响
- 待办：创建 SmartUILinkageLab、ModularAssemblyLab、RenderStatePlayground 组件

## 2026-05-06 ADP imperfection root cause
- Root cause found: ADPMCP writes full `slides.json` across all modules, but StoryboardMCP, DesignMCP, and StitchMCP still filter to the current module and overwrite one global contract file.
- Current data confirms only `Module_04` is covered by `storyboard-contract.json`, `design-contract.json`, and `stitch-manifest.json`; Module_01~03 slides are missing from those contracts.
- `scripts/verify_course.py` previously passed because it did not enforce full slide coverage for storyboard/design/stitch contracts.
- Added coverage checks to both `scripts/verify_course.py` and `.agent/templates/scripts/verify_course.py`; verification now correctly fails until ADP contract generation is made full-coverage or per-module contract files are introduced.

## 2026-05-06 ADP as MVP dispatcher
- DAG decision: ADP is no longer a parallel full-writer node. `--adp` now dispatches modules from `.agent/adp-scope.json` and runs the normal `MVPMCP` complete-module pipeline for each target module.
- Code changed: `.agent/flow_engine.py` no longer calls `ADPMCP.generate_products()`; ADP target expansion reads `.agent/adp-scope.json`. `.agent/mcp_servers/adp_mcp.py` now fail-fast deprecates `generate_products()`.
- Scope changed: `.agent/mvp-scope.json` now records complete module slide ranges, aligned with ADP full-module execution.
- Rules/docs updated: `docs/Skill_Chain_DAG.md`, `docs/ADP_Execution_Contract.md`, `.agent/rules.md`, `.agent/SKILL.md`, `.agent/handoff/HANDOFF_PROTOCOL.md`.
- Verification: py_compile passed; platform guard passed; ADP target expansion returns Module_01~Module_04. `verify_course.py` currently fails by design because existing generated products still reflect the old mixed ADP state; rerun MVP/ADP via the current execution-layer platform to regenerate.

## 2026-05-06 Runtime role switching
- Added role switching rule: roles are runtime responsibilities, not hard platform bindings.
- User can say `你是 workflow/DAG/工作流层`, `你是 product/执行层/产物层`, or `你是 review/检查层`; the current platform switches immediately.
- Workflow role touches workflow assets; product role executes MVP/ADP and product-facing checks through DAG/template/MCP; review role inspects only unless repair is requested.
- Durable coordination remains in `.agent/`; no `.cursor/` or `.workbuddy/` project knowledge directories.

## 2026-05-06 Course home module metadata alignment
- Fixed course home inconsistency where Module_02/03 displayed fallback titles (`Module_02`, `Module_03`) and empty summaries while Module_04 had semantic metadata.
- Root cause: module source `course.json` files used mixed schemas. `MVPMCP` only read `course.module`, so top-level `moduleId/title/description` and `modules[]` fell back to module ids on regeneration.
- Updated `CourseContent/Module_02/course.json`, `CourseContent/Module_03/course.json`, and `CourseContent/Module_04/course.json` to the standard `module` schema.
- Hardened `.agent/mcp_servers/mvp_mcp.py` with `_module_metadata()` so MVP accepts `module`, `modules[]`, and legacy top-level `moduleId/title/description` without falling back to generic ids.
- Synced current generated `CourseApp/src/data/course.json` so the open home page shows consistent module titles and summaries immediately.
- Verification: `python -m py_compile .agent\mcp_servers\mvp_mcp.py` passed; `npm --prefix CourseApp run build` passed. `python scripts\verify_course.py` still fails on known old storyboard/design/stitch coverage debt unrelated to this metadata fix.

## 2026-05-06 ADP accumulation semantics
- ADP remains a dispatcher, but now uses progressive accumulation instead of repeatedly replacing the previous module product.
- `flow_engine.py --adp` does one initial cleanup with `clear_adp_accumulation_outputs()`, then runs each module through `MVPMCP.generate_products(accumulate=True, clean=False, scope_file_name="adp-scope.json")`.
- Ordinary MVP is unchanged: it still uses MVP cleanup and scoped single-module output.
- Accumulated ADP product surfaces: CourseApp data (`course/slides/quizzes/explorations`), `storyboard-contract.json`, `design-contract.json`, and `stitch-manifest.json`.
- This prevents later modules from removing previous module manifests while preserving MVP's original removal/scope behavior.

## 2026-05-06 Course home module progress state
- Workflow-layer template change: `.agent/templates/course-app/src/views/CourseHome.vue` now defines module card status controls for `看过`, `学过`, `已做题`, and `掌握`.
- State semantics are ordered and dependency-based: one numeric level represents the highest reached step, so later states imply earlier states.
- Persistence is local learner state in browser `localStorage` under `shadergui-module-progress-v1`.
- No learner progress is written into `CourseContent/` or generated data contracts.
- Verification gates were added to `scripts/verify_course.py` and `.agent/templates/scripts/verify_course.py`; generated `CourseApp` should be refreshed by MVP/ADP.

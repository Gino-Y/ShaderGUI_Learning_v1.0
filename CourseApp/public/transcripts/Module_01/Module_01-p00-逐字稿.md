当我们谈论 ShaderGUI 时，先不要把它理解成给 Inspector 换一层皮。
更准确地说，它是 Shader 参数系统的前端工程层。Shader 负责底层渲染逻辑，ShaderGUI 负责把这些参数组织成美术能理解、能安全使用的操作界面。
ShaderGUI 的价值不只是让界面更好看。它真正承担的是三件事：把参数按使用逻辑组织起来，告诉使用者每个参数的意义，并在必要时自动同步材质状态。

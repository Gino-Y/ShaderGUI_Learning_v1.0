# 2026-05-05 工作记录

## 关键修复（防止节点漂移）

### 1. 修复 `.agent/mcp_servers/design_mcp.py`
- ✅ 补上缺失的 `design_slides` 
- ✅ 补上 `_infer_typography()` 函数
- ✅ 补上 `_infer_color_scheme()` 函数
- ✅ 修掉错误的 `any(any(...))` 逻辑
- ✅ 修掉被乱码注释吞掉的 `palette` 赋值
- ✅ 清掉几处导致 `SyntaxError` 的 mojibake 字符串

### 2. 修复 `.agent/mcp_servers/v0_mcp.py`
- ✅ 修掉坏 f-string
- ✅ 清掉影响执行的乱码字符串
- ✅ 给 v0 POST 增加可配置超时 `V0_API_TIMEOUT_SECONDS`
- ✅ 增加 `local-v0-fallback`：v0 远程 chat 超时/失败时，DAG 不再卡死，会生成本地 handoff 继续跑

### 3. 验证 flow engine
- ✅ 完整运行 `V0_API_TIMEOUT_SECONDS=5 python -u .agent\flow_engine.py --mode test --stage mvp --scope module --module Module_01 --basedir .`
- ✅ 到达 `DEPLOY_READY` 状态
- ✅ `python .agent\platform_violation_guard.py --basedir .` 通过
- ✅ `python scripts\verify_course.py` 通过
- ✅ `npm --prefix CourseApp run build` 通过

## 关键改进

1. **防止节点漂移**：通过修正 `mvp_mcp.py` 中的 `_copy_template_tree()`，确保重新生成 MVP 时不会覆盖自定义修改
2. **DAG 健壮性**：v0 API 超时/失败时，不再卡死，会使用 `local-v0-fallback` 继续运行
3. **Flow Engine 驱动**：所有 MCP 节点都通过 flow engine 调度，确保依赖关系正确

## 注意点

- 这次 v0 API key 可用，但远程 v0 chat 创建超时，所以本轮使用的是 `local-v0-fallback`，不是远程 v0 真实生成稿
- DAG 现在不会因此卡死了

## 下一步建议

1. **编写交接文档**：记录修复的关键问题和解决方案
2. **完整 MVP 测试**：使用 `local-v0-fallback` 生成完整的 Module_01 产物
3. **审计其他 MCP 服务器**：确保没有其他隐藏问题
